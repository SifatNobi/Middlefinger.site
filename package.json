{
  "name": "middlefinger-site",
  "version": "1.0.0",
  "description": "A Bolt-based project deployed on Netlify",
  "scripts": {
    "start": "bolt workspaces exec -- npm start",
    "build": "bolt workspaces exec -- npm run build",
    "test": "bolt workspaces exec -- npm test"
  },
  "workspaces": ["packages/*"],
  "dependencies": {
    "bolt": "^0.25.0"
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "license": "MIT"
}
